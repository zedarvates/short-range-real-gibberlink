<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Basic app permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Audio permissions for ultrasonic communication -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />

    <!-- Camera permissions for laser alignment and QR scanning -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="true" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />

    <!-- Vibration permissions for haptic feedback -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Biometric authentication for app security -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />

    <!-- Wake lock for continuous operation -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Hardware feature declarations for long-range components -->
    <uses-feature android:name="android.hardware.audio.low_latency" android:required="false" />
    <uses-feature android:name="android.hardware.audio.pro" android:required="false" />
    <uses-feature android:name="android.hardware.microphone" android:required="true" />
    <uses-feature android:name="android.hardware.bluetooth" android:required="false" />

    <!-- Long-range hardware specific permissions -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- For high-performance audio processing -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.GibberLink"
        android:usesCleartextTraffic="true"
        android:requestLegacyExternalStorage="true">

        <!-- Main activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.GibberLink">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Permission authorization activity for hardware access -->
        <activity
            android:name=".PermissionAuthorizationActivity"
            android:exported="false"
            android:theme="@style/Theme.GibberLink.NoActionBar"
            android:screenOrientation="portrait" />

        <!-- Face validation activity for human presence verification -->
        <activity
            android:name=".FaceValidationActivity"
            android:exported="false"
            android:theme="@android:style/Theme.NoTitleBar.Fullscreen"
            android:screenOrientation="portrait" />

        <!-- Range detection activity for ultrasonic ranging and laser control -->
        <activity
            android:name=".RangeDetectionActivity"
            android:exported="false"
            android:theme="@style/Theme.GibberLink.NoActionBar"
            android:screenOrientation="portrait" />

        <!-- Background service for continuous hardware monitoring -->
        <service
            android:name=".HardwareMonitoringService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="camera|microphone" />

        <!-- Broadcast receiver for hardware events -->
        <receiver
            android:name=".HardwareEventReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="com.gibberlink.HARDWARE_EVENT" />
                <action android:name="com.gibberlink.SAFETY_VIOLATION" />
                <action android:name="android.intent.action.BATTERY_LOW" />
            </intent-filter>
        </receiver>

        <!-- Meta-data for hardware capabilities -->
        <meta-data
            android:name="com.gibberlink.long_range_supported"
            android:value="true" />

        <meta-data
            android:name="com.gibberlink.ultrasonic_beam_supported"
            android:value="true" />

        <meta-data
            android:name="com.gibberlink.laser_modulation_supported"
            android:value="true" />

        <meta-data
            android:name="com.gibberlink.parametric_audio_supported"
            android:value="true" />

        <meta-data
            android:name="com.gibberlink.hardware_safety_monitoring"
            android:value="true" />

    </application>

</manifest>
